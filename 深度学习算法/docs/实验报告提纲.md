# 实验报告提纲

## 1. 实验目标

说明 CIFAR-10 十分类任务，以及 CNN 与 ResNet-18 双模型对比目标。

## 2. CNN 算法介绍

描述卷积层、BatchNorm、ReLU、池化、Dropout 与全连接分类器。

## 3. ResNet-18 算法介绍

说明残差连接 `H(x)=F(x)+x`、迁移学习、预训练权重和最后分类层替换。

## 4. CIFAR-10 数据集说明

列出 10 个类别、训练集、测试集、图片尺寸和通道数。

## 5. 数据预处理

分别说明 CNN 的 32x32 预处理和 ResNet-18 的 224x224 ImageNet 归一化。

## 6. 模型训练

记录训练参数、优化器、学习率调度器、Early Stopping 和硬件环境。

## 7. 两算法结果对比

预留真实结果表格：Accuracy、Precision、Recall、F1、AUC、参数量、平均推理时间。不得填写未训练得到的虚假数值。

## 8. 系统集成与交互界面

说明 FastAPI 接口、Vue 页面、单图预测、批量预测和图表展示。

## 9. 总结与不足

分析模型表现、训练成本、推理速度和后续改进方向。

